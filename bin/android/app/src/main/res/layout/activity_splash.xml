<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/splash_background">

    <!-- Animated Logo Container -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:orientation="vertical">

        <!-- Logo with Pulse Animation -->
        <ImageView
            android:id="@+id/ivLogo"
            android:layout_width="140dp"
            android:layout_height="140dp"
            android:src="@drawable/ic_splash_logo"
            android:contentDescription="App Logo"
            android:elevation="8dp" />

        <!-- App Name with Animation -->
        <TextView
            android:id="@+id/tvAppName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="28dp"
            android:text="🧠 MentalBook"
            android:textAppearance="@style/TextAppearance.VirtualXander.Splash.Title"
            android:shadowDx="0"
            android:shadowDy="2"
            android:shadowRadius="4"
            android:shadowColor="#40000000" />

        <!-- Tagline with Animation -->
        <TextView
            android:id="@+id/tvTagline"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="✨ Your Mental Wellness Companion ✨"
            android:textAppearance="@style/TextAppearance.VirtualXander.Splash.Subtitle"
            android:alpha="0.95" />

    </LinearLayout>

    <!-- Creative Loading Indicator -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="100dp"
        android:gravity="center"
        android:orientation="vertical">

        <com.google.android.material.progressindicator.CircularProgressIndicator
            android:id="@+id/progressBar"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:indeterminate="true"
            app:indicatorColor="@android:color/white"
            app:trackColor="#40FFFFFF"
            app:trackThickness="4dp"
            app:indicatorSize="48dp" />

        <TextView
            android:id="@+id/tvLoading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Loading wonderful experiences..."
            android:textAppearance="@style/TextAppearance.VirtualXander.Splash.Version"
            android:alpha="0.8" />

    </LinearLayout>

    <!-- Version Info -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="32dp"
        android:text="🚀 Version 0.2.0.2"
        android:textAppearance="@style/TextAppearance.VirtualXander.Splash.Version" />

</RelativeLayout>

